# docker-compose 部署用环境变量
# 复制为 .env 并填写真实值：cp .env.example .env

# 会话签名密钥：生产务必改成随机长字符串
# 生成示例：openssl rand -hex 32
TOMO_SESSION_SECRET=

# 首次启动创建的管理员账号（登录后请尽快改密码）
TOMO_ADMIN_USER=admin
TOMO_ADMIN_PASSWORD=

# Git 提交作者信息（可选）
TOMO_GIT_AUTHOR_NAME=Tomo
TOMO_GIT_AUTHOR_EMAIL=tomo@local
